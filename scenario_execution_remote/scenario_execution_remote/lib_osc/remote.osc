import osc.types

modifier remote:
    # Execute the wrapped action on a remote scenario-execution-server.
    # endpoint examples:
    #   "192.168.1.10"       TCP, default port 7613
    #   "192.168.1.10:9000"  TCP, explicit port
    #   "/tmp/se.sock"       Unix domain socket
    endpoint: string
